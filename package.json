{
  "name": "webcat",
  "version": "1.0.0",
  "description": "Terminal-native AI swarm for authorized web application security assessment",
  "license": "MIT",
  "type": "module",
  "bin": { "webcat": "./webcat.mjs" },
  "scripts": {
    "start": "node webcat.mjs",
    "test": "node webcat.mjs --version && node webcat.mjs --help"
  },
  "engines": { "node": ">=22.0.0" }
}
