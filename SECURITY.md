# Security Policy

WebCat is for explicitly authorized assessments only. Active MCP calls require confirmed authorization, exact scope, mode permission, and operator approval where required. Destructive operations are disabled by default. Secrets are redacted before persistence.
