version: 2
updates:
  # WebCat has no runtime npm dependencies, but the CI actions themselves are a supply-chain
  # surface worth keeping pinned and current.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
