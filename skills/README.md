# WebCat Skills

Skills are Markdown methodology modules with YAML frontmatter. WebCat discovers them from:

1. repository `skills/`;
2. user `~/.webcat/skills/`;
3. project `.webcat/skills/`.

Later sources override earlier sources by skill name. Skills guide agent methodology; they do not bypass scope, approval, or MCP enforcement.
